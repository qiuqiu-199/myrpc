<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">


    <!--扫描包路径-->
    <context:component-scan base-package="io.qrpc.demo"/>

    <!--配置文件-->
    <context:property-placeholder location="classpath:rpc.properties"/>

    <!--注册bean-->
    <bean id="rpcServer" class="io.qrpc.provider.spring.RpcSpringServer">
        <constructor-arg name="serverAddr" value="${server.address}"/>
        <constructor-arg name="registryType" value="${registry.type}"/>
        <constructor-arg name="registryAddr" value="${registry.address}"/>
        <constructor-arg name="registryLoadBalanceType" value="${registry.loadbanlance.type}"/>
        <constructor-arg name="reflectType" value="${reflect.type}"/>
        <constructor-arg name="heartbeatInterval" value="${server.heartbeatInterval}"/>
        <constructor-arg name="scanNotActiveChannelInterval" value="${server.scanNotActiveChannelInterval}"/>
        <constructor-arg name="enableCacheResult" value="${cacheResult.enable}"/>
        <constructor-arg name="cacheResultExpire" value="${cacheResult.expire}"/>
        <constructor-arg name="maxConnectionCount" value="${connectionManage.maxConnectionCount}"/>
        <constructor-arg name="disuseStrategyType" value="${connectionManage.disuseStrategy}"/>
        <constructor-arg name="enableRateLimiter" value="${rateLimiter.enableRateLimiter}"/>
        <constructor-arg name="rateLimiterType" value="${rateLimiter.rateLimiterType}"/>
        <constructor-arg name="permits" value="${rateLimiter.permits}"/>
        <constructor-arg name="milliSeconds" value="${rateLimiter.milliSeconds}"/>
    </bean>
</beans>